# EXECUTION

## PLAN
- Keep the repo clone-first for local use while adding the minimum npm metadata required to publish `ozcar` as a Pi package.
- Align the package contract with upstream Pi package conventions: discoverable keywords, explicit publish metadata, and a tight tarball surface.
- Preserve the existing `.pi` manifest and source-backed extension layout so local `pi -e /Users/x/ozcar` and future `pi install npm:...` both load the same package surface.
- Add validation that the package manifest continues to advertise the Pi resources and npm publish metadata expected by downstream Pi package discovery.

## CHANGES
- Updated `package.json` to use the scoped npm package name `@amarsinghcodes/pi-ozcar`, removed the old private-only publish posture, added Pi-friendly discoverability keywords, declared a tight `files` allowlist, and added `repository`, `author`, `license`, `bugs`, `homepage`, and `publishConfig.access`.
- Kept the repo on its existing module contract instead of forcing `type: module`, because the current TypeScript build still relies on Node16-style relative imports without explicit `.js` extensions and a metadata-only turn should not trigger a repo-wide ESM migration.
- Added `LICENSE` with an MIT license so the repo and npm package have an explicit open-source license.
- Updated `README.md` to document the clone-first local path flow and the eventual Pi npm install flow using `npm:@amarsinghcodes/pi-ozcar`.
- Extended `tests/pi-extension.test.ts` so the package-manifest test now covers the scoped package name, publish metadata, and tarball allowlist alongside the existing `pi` manifest assertions.

## VALIDATION
- `npm install --package-lock-only` -> passed
- `npm run build` -> passed
- `npm test` -> passed (`6` files passed, `1` skipped; `42` tests passed, `1` skipped)
- `npm pack --dry-run` -> passed; tarball renamed to `@amarsinghcodes/pi-ozcar`, reduced to `29` files / `25.7 kB`, and now contains only the intended `.pi/`, `src/`, `README.md`, `LICENSE`, and `package.json` publish surface

## REFERENCE
- PLAN: docs/plans/2026-04-13T01-50-43Z_PLAN.md

## OPEN QUESTIONS
None

## NEXT
Ready for review or publish prep: the repo remains clone-first for local `pi -e /Users/x/ozcar`, and the npm package metadata now matches the Pi package install surface without shipping the repo's internal docs, tests, or historical artifacts.
