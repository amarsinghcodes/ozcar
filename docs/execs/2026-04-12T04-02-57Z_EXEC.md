# EXECUTION

## PLAN
- Implement only Phase 6 from `PLAN.md`: add a live provider runtime seam with actionable preflight, retryable CLI execution, and durable raw provider artifacts for `plan` and `scan`.
- Keep the core runtime TypeScript-only and preserve the current JSON-first contracts, durable run store, `--dry-run` path, and replayable fixture seam while extending non-dry execution.
- Thread the live seam through `src/providers/*`, `src/phases/{plan,scan}.ts`, and the operational commands that depend on those phases, then keep `resume`, `replay`, `rebuild`, and `doctor` coherent with the new live artifacts.
- Add deterministic seam coverage for preflight, retry, parser behavior, failure handling, artifact persistence, and one end-to-end non-dry proof plus a dry-run proof.
- Validate with `npm run build`, `npm test`, the exact Phase 6 seam-focused tests added in this slice, and at least one end-to-end non-dry run outcome.

## CHANGES
- Added the Phase 6 live-provider contract in `src/contracts/provider-execution.ts`, `src/providers/runtime.ts`, and `src/gates/provider-execution.ts`, including run-level `provider/preflight.json` plus per-invocation attempt artifacts with `prompt.md`, `stdout.txt`, `stderr.txt`, `response.txt`, `schema.json`, and `execution.json`.
- Upgraded `src/providers/{base,codex,claude}.ts` from metadata-only definitions to typed CLI-backed adapters with command detection, auth and version preflight, invocation metadata, retry classification, and parsed response extraction for structured plan and scan payloads.
- Updated `src/phases/{plan,scan}.ts` to support both dry-run and live execution while preserving canonical `plan.json`, `request.json`, and `output.json`, adding live plan or scan persistence under the existing loop and scan roots, and reusing stored prompt plus request inputs for live replay or resume repair.
- Updated `src/gates/{plan,scan}.ts` and the operational commands in `src/commands/{run,resume,replay}.ts` so `run` performs explicit preflight once per live run, `resume` and `replay` can rerun missing or broken live scan outputs, and `doctor` validates the new preflight and attempt artifacts through the tightened gates.
- Tightened the prompt surface in `src/prompts/{planner,scanner}.md`, extended direct phase callers in `tests/{phase2,phase4}.test.ts`, and added deterministic Phase 6 seam coverage plus a fake live-provider fixture in `tests/phase6.test.ts` and `tests/fixtures/phase6/providers/claude/bin/claude`.

## VALIDATION
- `npm run build` -> passed (`tsc -p tsconfig.build.json`).
- `npx vitest run tests/phase6.test.ts` -> passed (`1` file, `5` tests passed). Coverage included actionable codex preflight failure persistence, live plan retry and parser unwrap behavior, live `run` plus `resume` plus `replay` plus `rebuild` plus `doctor` coherence, explicit non-dry preflight failure guidance, and `--dry-run` compatibility with auth intentionally missing.
- `npm test` -> passed (`7` test files, `42` tests passed).
- `env PATH="/Users/x/oz/ozcar/tests/fixtures/phase6/providers/claude/bin" OZCAR_FAKE_CLAUDE_AUTH=ready /opt/homebrew/bin/node dist/cli.js run --root /tmp/ozcar-phase6-proof-live --target-root /tmp/ozcar-phase6-proof-live/target --run-id proof-live --provider claude --scope Sample.sol --scan-target Sample.sol --research-direction "Investigate balance accounting invariants."` -> passed. Observed `plan.mode: "live"`, `scans[0].mode: "live"`, persisted `/tmp/ozcar-phase6-proof-live/.ai-auditor/runs/proof-live/provider/preflight.json`, and completed triage plus validation outputs for `3` live findings.
- `env PATH="/Users/x/oz/ozcar/tests/fixtures/phase6/providers/claude/bin" OZCAR_FAKE_CLAUDE_AUTH=missing /opt/homebrew/bin/node dist/cli.js run --root /tmp/ozcar-phase6-proof-dry --target-root /tmp/ozcar-phase6-proof-dry/target --run-id proof-dry --provider claude --dry-run --scope Sample.sol --scan-target Sample.sol --research-direction "Investigate balance accounting invariants." --finding-fixture /Users/x/oz/ozcar/tests/fixtures/phase3-scan-findings.json` -> passed. Observed `plan.mode: "dry-run"`, `scans[0].mode: "dry-run"`, and `provider.preflightFile: null` even though provider auth was intentionally missing.

## REFERENCE
- PLAN: docs/plans/2026-04-12T03-57-14Z_PLAN.md

## OPEN QUESTIONS
None

## NEXT
Start the Phase 6 review slice against this execution artifact. Readiness: ready for review.
