# EXECUTION

## PLAN
- Extend the Phase 8 export contract with authoritative reported duration, cost, input-token, and output-token fields sourced only from ozcar contract data.
- Keep the existing snapshot -> stored contracts -> rebuild -> `exports/findings.json` seam intact so reported metrics rebuild deterministically from stored JSON.
- Preserve the validated-only findings surface and keep measured wall-clock concerns external to ozcar's reported metrics.
- Add focused docs, fixtures, and regression coverage for missing, partial, and full reported-metric export cases.

## CHANGES
- Extended `src/contracts/audit.ts` with an optional normalized `reportedMetrics` block for authoritative `durationSeconds`, `costUsd`, `inputTokens`, and `outputTokens` facts. Empty blocks normalize away so missing metrics stay deterministic.
- Extended `src/contracts/export.ts` and `src/reports/export-findings.ts` so `exports/findings.json` mirrors those authoritative reported metrics at the top level without changing the validated-only findings surface or `generatedAt` derivation.
- Updated `.pi/extensions/ozcar/commands/export.ts` to describe whether authoritative reported metrics were exported while keeping measured wall-clock and time-budget assumptions external.
- Added Phase 8 regression coverage in `tests/audit-artifacts.test.ts` and `tests/pi-extension.export.test.ts` for missing, partial, and full reported-metric exports, including deterministic rebuild behavior.
- Added Phase 8 golden fixtures under `tests/fixtures/phase8/` and updated repo-local docs in `README.md` plus `.pi/skills/ozcar-audit/SKILL.md` to describe the new optional `reportedMetrics` contract and the continued external follow-up for downstream adapter reads.

## VALIDATION
- `npm test -- tests/audit-artifacts.test.ts` -> passed (`12` tests)
- `npm test -- tests/pi-extension.export.test.ts` -> passed (`6` tests)
- `npm run build` -> passed
- `npm test` -> passed (`12` files passed, `1` skipped; `79` tests passed, `1` skipped)

## REFERENCE
- PLAN: docs/plans/2026-04-13T01-50-43Z_PLAN.md

## OPEN QUESTIONS
None

## NEXT
Ready for review: Phase 8 reported-metrics export is implemented and validated in-repo; downstream `../auditor-bench` adapter consumption of the new `reportedMetrics` block remains external follow-up.
