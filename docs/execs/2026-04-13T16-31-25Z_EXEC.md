# EXECUTION

## PLAN
- Keep the README simple while making the Pi-owned runtime versus `ozcar`-owned audit contract explicit.
- Integrate the end-to-end auditor flow into the existing quickstart and workflow sections instead of creating a separate design document.
- Clarify the canonical comparison surface, validated-only export discipline, and authoritative reported-metrics semantics in user-facing terms.

## CHANGES
- Updated `README.md` to add a concise `How The Auditor Works` section covering the live Pi session runtime, branch labels, `/tree` recovery behavior, checkpoint parity between human and agent paths, and the export materialization flow.
- Tightened `README.md` wording around the on-disk artifact surface so the file now describes the JSON-first contract more explicitly and frames Markdown as mechanically rebuilt output.
- Expanded the `Comparison Contract` rules in `README.md` to call out authoritative `reportedMetrics`, external measured wall-clock assumptions, and strict fail-closed snapshot validation without turning the README into an internal code tour.

## VALIDATION
- `npm test -- tests/pi-extension.test.ts tests/pi-extension.audit.test.ts tests/pi-extension.export.test.ts tests/pi-extension.providers.test.ts tests/audit-artifacts.test.ts` -> passed (`5` files, `38` tests)

## REFERENCE
- PLAN: docs/plans/2026-04-13T01-50-43Z_PLAN.md

## OPEN QUESTIONS
None

## NEXT
Ready for review: the README now explains the auditor end-to-end in the same document users already land on, while keeping the package-first quickstart and stable export contract front and center.
