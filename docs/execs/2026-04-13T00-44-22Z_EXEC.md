# EXECUTION

## PLAN

- Verify that the current Phase 6 export seam already exposes a deterministic validated-only `findings.json` from stored audit artifacts, then isolate the minimum downstream comparison gap still missing.
- Keep the Phase 6 implementation inside the stable export surface, adapter-facing notes or fixtures, and explicit comparison assumptions instead of reshaping the underlying audit, finding, triage, or validation contracts.
- Make only the smallest code touch needed to harden the export surface for downstream consumers if a real ambiguity or instability exists.
- Add deterministic tests and fixtures that prove downstream comparison can consume exported validated findings without Pi transcript scraping or internal contract reshaping.
- Validate the slice with `npm run build`, `npm test`, and any focused Phase 6 proof added by this work.

## CHANGES

- Verified that the current `src/contracts/export.ts`, `src/reports/export-findings.ts`, and rebuild/store seam already produced a deterministic validated-only `.ai-auditor/audits/<audit-id>/exports/findings.json`; the missing Phase 6 seam was downstream-facing contract framing, not internal JSON reshaping.
- Updated [.pi/extensions/ozcar/commands/export.ts](/Users/x/oz/ozcar/.pi/extensions/ozcar/commands/export.ts) so `/ozcar-audit-export` now describes and reports the stable `findings.json` comparison surface explicitly, including the validated-only source discipline and the requirement to capture provider, model, and time-budget assumptions separately from Pi or the benchmark harness.
- Updated [.pi/extensions/ozcar/index.ts](/Users/x/oz/ozcar/.pi/extensions/ozcar/index.ts) so `/ozcar` help points users at `exports/findings.json` as the stable downstream comparison surface.
- Updated [README.md](/Users/x/oz/ozcar/README.md) with a concise Phase 6 comparison-contract note that pins `exports/findings.json` as the canonical downstream input and makes the provider/model/time-budget assumptions explicit without changing the export contract.
- Added the golden fixture [tests/fixtures/phase6/findings-export.expected.json](/Users/x/oz/ozcar/tests/fixtures/phase6/findings-export.expected.json) to pin the byte-stable `findings.json` surface downstream adapters should consume.
- Updated [tests/audit-artifacts.test.ts](/Users/x/oz/ozcar/tests/audit-artifacts.test.ts), [tests/pi-extension.export.test.ts](/Users/x/oz/ozcar/tests/pi-extension.export.test.ts), and [tests/pi-extension.test.ts](/Users/x/oz/ozcar/tests/pi-extension.test.ts) to cover the pinned Phase 6 export bytes plus the explicit command/help guidance for validated-only export consumption and external provider/model/time-budget assumptions.

## VALIDATION

- `rg -n "ozcar|findings.json|validated findings|validated_findings|comparison|time budget|time_budget|provider|model" /Users/x/oz/auditor-bench /Users/x/oz/fuzzing-team-foz` -> confirmed the current downstream comparison harness still documents and implements the legacy `.ai-auditor/runs/<run-id>/loops/<loop>/validated/...` ozcar adapter seam, so the minimum in-repo Phase 6 gap was stable export-surface guidance and fixtures for `.ai-auditor/audits/<audit-id>/exports/findings.json`.
- `npm test -- tests/audit-artifacts.test.ts tests/pi-extension.export.test.ts tests/pi-extension.test.ts` -> passed (`3` files, `15` tests) and covered the new pinned export fixture plus the Phase 6 command/help messaging.
- `npm run build` -> passed.
- `npm test` -> passed (`12` files passed, `1` file skipped; `69` tests passed, `1` skipped test for the opt-in interactive Pi proof).

## REFERENCE

- PLAN: docs/plans/2026-04-13T00-40-06Z_PLAN.md

## OPEN QUESTIONS

None

## NEXT

This Phase 6 slice is ready for review. The next safe follow-up is downstream integration work outside this repo to switch comparison adapters from legacy validated-bundle reads to `.ai-auditor/audits/<audit-id>/exports/findings.json` while continuing to capture provider, model, and time-budget assumptions externally. Readiness: ready for review.
