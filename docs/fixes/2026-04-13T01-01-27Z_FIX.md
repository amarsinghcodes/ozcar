# FIX

## PLAN

- Root cause: the new zero-validated export fallback in `src/reports/export-findings.ts` is implemented but untested, so a later Phase 6 change could regress the `audit.createdAt` fallback without tripping the current suite.
- Add one small deterministic export test that materializes an audit snapshot with zero validated findings and asserts the exported `generatedAt` falls back to `audit.createdAt`.
- Keep the follow-up strictly inside Phase 6 regression coverage; do not change export contracts or production code unless the test exposes a real defect.
- Re-run the focused export proof, `npm run build`, and the full test suite.

## CHANGES

None yet.

## VALIDATION

Not run

## RISKS

None

## REFERENCE

- REVIEW: docs/reviews/2026-04-13T00-51-42Z_REVIEW.md
