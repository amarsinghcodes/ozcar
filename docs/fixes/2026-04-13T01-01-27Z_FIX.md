# FIX

## PLAN

- Root cause: the new zero-validated export fallback in `src/reports/export-findings.ts` is implemented but untested, so a later Phase 6 change could regress the `audit.createdAt` fallback without tripping the current suite.
- Add one small deterministic export test that materializes an audit snapshot with zero validated findings and asserts the exported `generatedAt` falls back to `audit.createdAt`.
- Keep the follow-up strictly inside Phase 6 regression coverage; do not change export contracts or production code unless the test exposes a real defect.
- Re-run the focused export proof, `npm run build`, and the full test suite.

## CHANGES

- Updated [tests/audit-artifacts.test.ts](/Users/x/oz/ozcar/tests/audit-artifacts.test.ts) with one small Phase 6 regression that materializes a snapshot with zero validated findings and asserts the exported `findings.json` falls back to `audit.createdAt` while serializing an empty `findings` array.
- Kept this follow-up coverage-only. No production code or export-contract fields changed because the existing `audit.createdAt` fallback in `src/reports/export-findings.ts` already behaved correctly.

## VALIDATION

- `npm test -- tests/audit-artifacts.test.ts -t "falls back to audit.createdAt when no validated findings are exported"` -> passed (`1` file, `1` test, `9` skipped).
- `npm run build` -> passed.
- `npm test` -> passed (`12` files passed, `1` skipped; `71` tests passed, `1` skipped).

## RISKS

- `/Users/x/oz/auditor-bench/src/auditor_benchmark/adapters/ozcar.py` still consumes the legacy `.ai-auditor/runs/.../validated/...` seam, so downstream comparison integration remains follow-up work outside this repo.

## REFERENCE

- REVIEW: docs/reviews/2026-04-13T00-59-39Z_REVIEW.md
