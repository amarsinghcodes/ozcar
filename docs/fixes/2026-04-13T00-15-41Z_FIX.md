# FIX

## PLAN

- Repair the preset validator so it matches Pi's canonical `provider/modelId` contract by splitting on the first slash only and allowing additional `/` characters inside the model id.
- Add deterministic slash-containing preset coverage plus a live Pi RPC proof so the regression stays guarded at both the helper and real editor-staging seams.
- Keep the repair inside the Phase 5 preset helper, command surface, and tests without widening auth, streaming, provider-runtime, or Phase 6 scope.

## CHANGES

- Widened `.pi/extensions/ozcar/providers/presets.ts` so canonical preset refs are parsed on the first slash only, preserving slash-containing model ids such as `openrouter/unsloth/qwen3.5-35b-a3b` while still rejecting whitespace-broken or multi-line values.
- Updated `tests/pi-extension.providers.test.ts` so the deterministic positive path now uses a valid slash-containing model reference and directly asserts that `resolveAuditModelPreset()` keeps it as `model` instead of downgrading it to `invalidModel`.
- Updated `tests/pi-extension.integration.test.ts` so the real Pi RPC proof now stages `/ozcar-audit-model balanced` with a slash-containing model id and waits for the matching `set_editor_text` payload from Pi.

## VALIDATION

- `npm run build` -> passed.
- `npm test -- tests/pi-extension.providers.test.ts tests/pi-extension.integration.test.ts` -> passed (`2` files, `11` tests).
- `npm test` -> passed (`12` files, `68` tests; `1` additional test file skipped with `1` skipped test for the opt-in interactive Pi proof).

## RISKS

- The provider segment still assumes Pi provider ids stay within `[A-Za-z0-9._-]`, which matches the current built-in provider names and the current extension contract.

## REFERENCE

- REVIEW: docs/reviews/2026-04-13T00-14-03Z_REVIEW.md
