# FIX

## PLAN

- Root cause 1: the new cross-host lock regression still hardcodes `"other-host"`, which means the test is no longer strictly environment-agnostic if it ever runs on a machine with that hostname.
- Root cause 2: the conservative lock behavior is only valuable if the test fixture itself guarantees non-local ownership instead of assuming one literal hostname is foreign everywhere.
- Update the cross-host lock helper in `tests/run-store.test.ts` so it derives a hostname guaranteed to differ from `os.hostname()` and returns it to the test for assertion.
- Revalidate with the standard Phase 1 build and test commands and record the result in a new fix artifact.

## CHANGES

- Updated [tests/run-store.test.ts](/Users/x/oz/ozcar/tests/run-store.test.ts) so the cross-host lock regression now gets its expected foreign hostname from `writeCrossHostLock()` instead of hardcoding `"other-host"` in the test body.
- Updated [tests/run-store.test.ts](/Users/x/oz/ozcar/tests/run-store.test.ts) so `writeCrossHostLock()` derives a guaranteed non-local hostname from `os.hostname()` via `deriveForeignHostname()` and returns it for assertion.

## VALIDATION

- `npm run build` -> passed (`tsc -p tsconfig.build.json`).
- `npm test` -> passed (`1` test file, `8` tests passed).
- `rg -n "docs/reviews/2026-04-12T00-06-07Z_REVIEW.md" docs/fixes/2026-04-12T00-09-58Z_FIX.md` -> matched `24:- REVIEW: docs/reviews/2026-04-12T00-06-07Z_REVIEW.md`.

## RISKS

- This slice only improves test portability; runtime lock behavior is unchanged.

## REFERENCE

- REVIEW: docs/reviews/2026-04-12T00-06-07Z_REVIEW.md
