# FIX

## PLAN

- Root cause 1: the new stale-lock reclaim path is only safe because it reclaims a lock when same-host dead-pid ownership is proven; without a regression test, later edits could accidentally widen that rule and steal a cross-host or otherwise non-provable lock.
- Root cause 2: the current lock suite proves positive stale-lock reclaim and active-lock rejection, but it does not yet pin the conservative non-reclaim branch next to the new reclaim behavior.
- Add one focused regression test in `tests/run-store.test.ts` that seeds a cross-host lock with a dead pid and asserts `reopenRunStore()` still fails with `RunLockError`.
- Revalidate the Phase 1 seam with the standard build and test commands and record the new fix slice under `docs/fixes/`.

## CHANGES

- Added one conservative lock regression in [tests/run-store.test.ts](/Users/x/oz/ozcar/tests/run-store.test.ts) that seeds a cross-host dead-pid `.lock` and asserts `reopenRunStore()` still rejects it with `RunLockError` instead of reclaiming it.
- Added a small test helper in [tests/run-store.test.ts](/Users/x/oz/ozcar/tests/run-store.test.ts) to write cross-host lock metadata beside the existing same-host stale-lock helper so both branches stay explicit in the suite.

## VALIDATION

- `npm run build` -> passed (`tsc -p tsconfig.build.json`).
- `npm test` -> passed (`1` test file, `8` tests passed).
- `rg -n "docs/reviews/2026-04-11T23-39-09Z_REVIEW.md" docs/fixes/2026-04-12T00-03-12Z_FIX.md` -> matched `24:- REVIEW: docs/reviews/2026-04-11T23-39-09Z_REVIEW.md`.

## RISKS

- Invalid or cross-host lock metadata still require manual cleanup by design; this slice intentionally pins that conservative behavior rather than changing it.

## REFERENCE

- REVIEW: docs/reviews/2026-04-11T23-39-09Z_REVIEW.md
