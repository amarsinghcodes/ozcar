# FIX

## PLAN

- Root cause 1: `src/reports/export-findings.ts` derives `generatedAt` from every bundle timestamp in the snapshot, including pending and rejected bundles that are not serialized into `exports/findings.json`.
- Root cause 2: `tests/audit-artifacts.test.ts` pins canonical export bytes but never mutates only a non-validated bundle timestamp, so the validated-only stability claim was not actually exercised.
- Repair the export timestamp seam by deriving `generatedAt` only from the exported validated bundle set, with a stable fallback for empty validated exports.
- Add deterministic regression coverage that changes only non-validated bundle timestamps and proves `exports/findings.json` bytes remain unchanged.
- Re-run the focused Phase 6 proof, `npm run build`, and the full test suite.

## CHANGES

- Updated [src/reports/export-findings.ts](/Users/x/oz/ozcar/src/reports/export-findings.ts) so `buildFindingsExportDocument()` now derives `generatedAt` from the exported validated bundle set only, with a stable fallback to `audit.createdAt` when no validated findings exist. Pending and rejected bundle timestamps no longer perturb `exports/findings.json` bytes.
- Added the missing regression in [tests/audit-artifacts.test.ts](/Users/x/oz/ozcar/tests/audit-artifacts.test.ts) so Phase 6 now mutates only the non-validated bundle timestamps and asserts that `exports/findings.json` stays byte-identical with `generatedAt` pinned at `2026-04-12T20:08:00.000Z`.

## VALIDATION

- `npm test -- tests/audit-artifacts.test.ts -t "non-validated bundle timestamps change"` -> passed (`1` file, `1` test, `8` skipped) and verified pending-bundle timestamp churn no longer changes `exports/findings.json`.
- `npm test -- tests/audit-artifacts.test.ts tests/pi-extension.export.test.ts tests/pi-extension.test.ts` -> passed (`3` files, `16` tests).
- `npm run build` -> passed.
- `npm test` -> passed (`12` files passed, `1` skipped; `70` tests passed, `1` skipped).

## RISKS

- `/Users/x/oz/auditor-bench/src/auditor_benchmark/adapters/ozcar.py` still consumes the legacy `.ai-auditor/runs/.../validated/...` seam, so the remaining comparison integration follow-up is still downstream work outside this repo.

## REFERENCE

- REVIEW: docs/reviews/2026-04-13T00-51-42Z_REVIEW.md
