# FIX

## PLAN

- Root cause 1: the Phase 4 implementation added `validate.duplicate` and `validate.stale-location` branches in `src/store/run-health.ts`, but `tests/phase4.test.ts` stopped at stale reports and missing validation artifacts, so those reconciliation branches could regress without automated coverage.
- Root cause 2: the existing Phase 4 test helpers already produce a durable completed run fixture, so the missing coverage should be closed with two focused doctor tests that mutate the validated artifact layout instead of widening runtime code or adding new harness layers.

## CHANGES

- Expanded [tests/phase4.test.ts](/Users/x/oz/ozcar/tests/phase4.test.ts) with a duplicate-artifact doctor regression that copies `validated/0001-confirmed-high` into `pending/0001-confirmed-high` and asserts `validate.duplicate`.
- Expanded [tests/phase4.test.ts](/Users/x/oz/ozcar/tests/phase4.test.ts) with a stale-location doctor regression that moves `validated/0001-confirmed-high` into `pending/0001-confirmed-high` and asserts `validate.stale-location` alongside the expected missing-artifact `validate.gate` failure.
- Kept the fix scoped to Phase 4 seam coverage only; no runtime or contract code changed because the underlying `run-health` branches were already implemented correctly.

## VALIDATION

- `npx vitest run tests/phase4.test.ts` -> passed (`1` file, `8` tests passed), including the new duplicate and stale-location doctor coverage.
- `npm run build` -> passed (`tsc -p tsconfig.build.json`).
- `npm test` -> passed (`5` test files, `31` tests passed).

## RISKS

None

## REFERENCE

- REVIEW MESSAGE: phase4-doctor-duplicate-stale-location-coverage
