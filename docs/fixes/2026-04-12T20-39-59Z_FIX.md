# FIX

## PLAN

- Root cause 1: the Phase 2 tests and persisted validation only exercised startup discovery, so the slice claimed reload coherence without proving `resources_discover` on `reason: "reload"` or a real Pi reload round-trip.
- Root cause 2: the recorded `get_commands` proof used Pi's default project discovery, so it could not isolate whether `ozcar-audit` and `skill:ozcar-audit` came from `resources_discover`.
- Repair group 1: extend the extension tests to cover `resources_discover` for both `startup` and `reload`, then add an isolated Pi integration that drives a real `ctx.reload()` and rechecks the ozcar command surface after reload.
- Repair group 2: correct the persisted execution artifact so it cites the isolated explicit-path proof and the reload-capable test surface instead of the weaker startup-only validation.

## CHANGES

- Updated `tests/pi-extension.test.ts` so the `resources_discover` seam is exercised for both `startup` and `reload`.
- Added `tests/pi-extension.integration.test.ts`, which starts Pi with `--no-extensions --no-skills --no-prompt-templates -e ./.pi/extensions/ozcar/index.ts`, layers a temporary reload-helper extension, asserts `ozcar`, `ozcar-audit`, and `skill:ozcar-audit`, triggers `ctx.reload()`, and re-asserts the same isolated surface after reload.
- Updated `docs/execs/2026-04-12T20-08-04Z_EXEC.md` so its Phase 2 validation no longer overclaims the default-discovery `get_commands` proof and now records the isolated explicit-path startup proof plus reload-aware test coverage.

## VALIDATION

- `npm run build` -> passed (`tsc -p tsconfig.build.json && tsc -p tsconfig.pi.json`).
- `npm test` -> passed (`8` files, `42` tests).
- `pi --mode rpc --offline --no-session --no-extensions --no-skills --no-prompt-templates -e ./.pi/extensions/ozcar/index.ts` + `{"id":"isolated-commands","type":"get_commands"}` -> returned `ozcar`, `ozcar-audit`, and `skill:ozcar-audit`, with the prompt and skill entries sourced from `extension:index`.

## RISKS

- The reload round-trip integration is conditional on a local `pi` binary. Environments without `pi` still get the `startup`/`reload` unit seam, but not the subprocess-level reload proof.

## REFERENCE

- REVIEW: docs/reviews/2026-04-12T20-33-22Z_REVIEW.md
