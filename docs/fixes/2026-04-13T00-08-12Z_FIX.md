# FIX

## PLAN

- Close the review gap in `tests/pi-extension.integration.test.ts` by exercising `/ozcar-audit-model` through Pi RPC mode and asserting the real `set_editor_text` UI request emitted by `ctx.ui.setEditorText`.
- Harden the Phase 5 preset seam so `OZCAR_AUDIT_MODEL_*` only stages a repo-local Pi `/model` command when the configured value matches a safe single-line `<provider>/<model>` shape.
- Keep the repair inside the existing Phase 5 preset module, command surface, and deterministic tests without widening provider ownership or touching the transition-era standalone runtime.

## CHANGES

- Added a strict single-line `<provider>/<model>` validator in `.pi/extensions/ozcar/providers/presets.ts`, so malformed or multi-line `OZCAR_AUDIT_MODEL_*` values are treated as invalid configuration instead of being interpolated directly into a staged Pi `/model` command.
- Updated `.pi/extensions/ozcar/commands/model.ts` so invalid configured presets still stage the safe placeholder `/model <provider/model>` command and emit an explicit warning that the configured env value was ignored.
- Extended `tests/pi-extension.providers.test.ts` with malformed-value coverage for both command staging and help rendering, proving that unsafe env values no longer become staged live commands.
- Extended `tests/pi-extension.integration.test.ts` with a real Pi RPC proof for `/ozcar-audit-model balanced`, including the observed `extension_ui_request` with `method: "set_editor_text"` and the expected `/model openai/gpt-5-codex` payload.

## VALIDATION

- `npm run build` -> passed.
- `npm test -- tests/pi-extension.providers.test.ts tests/pi-extension.integration.test.ts` -> passed (`2` files, `10` tests).
- `npm test` -> passed (`12` files, `67` tests; `1` additional test file skipped with `1` skipped test for the opt-in interactive Pi proof).

## RISKS

- If Pi later supports provider ids that include characters outside `[A-Za-z0-9._-]`, the new preset validator in `.pi/extensions/ozcar/providers/presets.ts` will need a small follow-up widening.

## REFERENCE

- REVIEW: docs/reviews/2026-04-13T00-06-49Z_REVIEW.md
