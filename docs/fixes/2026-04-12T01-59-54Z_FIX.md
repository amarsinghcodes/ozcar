# FIX

## PLAN

- Route summarize-time validated artifact discovery through deterministic artifact identity from the validated directory name so malformed `finding.json` content is surfaced by the existing validation gate instead of by a raw parser exception.
- Rebuild confirmed findings text fields in a multiline-safe Markdown form so valid newline-bearing `summary`, `triage.rationale`, and `validation.rationale` values do not corrupt `confirmed-findings.md`.
- Extend the Phase 3 seam tests to cover both reported regressions directly: malformed validated artifacts during summarize and multiline confirmed-findings rendering.

## CHANGES

- Updated [src/phases/summarize.ts](/Users/x/oz/ozcar/src/phases/summarize.ts) to derive `scanId` and `findingId` from each validated artifact directory name before entering [src/gates/validate.ts](/Users/x/oz/ozcar/src/gates/validate.ts), and to throw `ValidationGateError` when the directory name itself is invalid.
- Updated [src/reports/confirmed-findings.ts](/Users/x/oz/ozcar/src/reports/confirmed-findings.ts) so multiline validated text fields render under quoted subsections instead of single-line bullets, and inline fields such as titles and locations are normalized to one line before being embedded in headings or bullets.
- Updated [src/reports/summary.ts](/Users/x/oz/ozcar/src/reports/summary.ts) to normalize inline multiline titles and locations before emitting the summary bullet list.
- Expanded [tests/phase3.test.ts](/Users/x/oz/ozcar/tests/phase3.test.ts) so summarize now has explicit regression coverage for malformed validated JSON and multiline confirmed-findings fields, and widened the local artifact helper to seed multiline locations and rationales.

## VALIDATION

- `npm run build` -> passed (`tsc -p tsconfig.build.json`).
- `npm test` -> passed (`4` test files, `23` tests passed).
- `npx vitest run tests/phase3.test.ts -t 'fails summarize with ValidationGateError when a validated artifact has malformed JSON'` -> passed and confirmed summarize now fails with typed `ValidationGateError`.
- `npx vitest run tests/phase3.test.ts -t 'renders multiline validated report fields without malformed confirmed-findings bullets'` -> passed and confirmed multiline validated report fields render safely.

## RISKS

None

## REFERENCE

- REVIEW: docs/reviews/2026-04-12T01-55-12Z_REVIEW.md
