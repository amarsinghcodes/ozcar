# PLAN

## GOAL

`PLAN.md` remains canonical, and the next active slice is now Phase 2: implement provider selection plus the `plan` and `scan` phases on top of the completed Phase 1 workspace and run-store foundation. The immediate goal is to land the smallest Phase 2 implementation that can emit durable loop `0001` plan and scan artifacts with deterministic gates and provider selection behind one small interface.

This repo still uses the local artifact workflow for persisted execution and review work. This plan artifact records the prompt-authoring handoff for the current Phase 2 slice and keeps later implementation and review packets pointed at the repo-local execution and review templates without replacing `PLAN.md`.

## SCOPE

### In Scope

- Phase 2 provider interface and adapters under `src/providers/{base,codex,claude}.ts`
- Phase 2 prompt sources under `src/prompts/{planner,scanner}.md`
- Phase 2 phase logic under `src/phases/{plan,scan}.ts`
- Phase 2 deterministic gates under `src/gates/{plan,scan}.ts`
- the minimal command wiring or validation seam needed to exercise provider selection and durable loop `0001` plan/scan artifacts without pulling Phase 4 operational flows forward
- tests and validation for provider detection or explicit override, replayable scan inputs, and deterministic gate failures

### Out of Scope

- Phase 3 triage, validate, summarize, or report generation work
- Phase 4 resume, replay, rebuild, or doctor completion beyond any minimal seam Phase 2 strictly needs for validation
- public plugin work, duplicated helper trees, or shell/Python orchestration

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `.agents/skills/artifact-md-workflow/SKILL.md` and the local templates for persisted execution and review artifacts
- keep the core runtime TypeScript-only
- keep machine contracts JSON-first
- keep providers behind one small interface driven by capability flags and defaults instead of provider-specific orchestration branches
- do not pull Phase 3+ behavior forward unless Phase 2 validation strictly requires a minimal supporting seam
- treat sibling repos under `../fuzzing-team-foz/` and `../apprentice-victor/` as read-only references

## PHASES

### Phase 1: Plan And Scan Implementation

Objective:
Implement the active `PLAN.md` Phase 2 slice so one loop can emit durable `plan.json` plus replayable scan artifacts through a small provider abstraction and deterministic gates.

Deliverables:
- provider base contract and `codex` / `claude` adapters
- planner and scanner prompt sources
- `plan` and `scan` phase modules
- `plan` and `scan` gate modules
- tests or fixtures that prove provider selection, durable artifacts, and deterministic failures

### Phase 2: Plan And Scan Review

Objective:
Verify the implementation against `PLAN.md`, the local artifact workflow, and the execution artifact before allowing later-phase work.

Deliverables:
- a new `docs/reviews/{UTC_TIMESTAMP}_REVIEW.md` targeting the Phase 2 execution artifact
- rerun build, test, and the closest exact dry invocation for loop `0001` plan/scan artifact creation
- findings on missing requirements, regressions, overengineering, or drift from the provider-and-gate contract

## VALIDATION

- `npm run build` must pass
- `npm test` must pass with added Phase 2 seam coverage
- provider detection and explicit override must be supported through one small interface
- a loop `0001` run must emit `plan.json` plus replayable scan artifacts under the run store
- gate failures must be deterministic and actionable
- the implementation slice must write `docs/execs/{UTC_TIMESTAMP}_EXEC.md` referencing this plan artifact
- the review slice must confirm Phase 2 scope only and rerun the recorded validation

## GATES

- the implementation slice may start immediately because Phase 1 acceptance criteria now pass and `PLAN.md` marks Phase 2 current
- the review slice may start only after the Phase 2 execution artifact exists and the implementation has run the recorded validation
- Phase 3 remains blocked until the Phase 2 review confirms provider selection, durable plan/scan artifacts, and deterministic gates

## REFERENCE

- REQUEST: prompt-bank pup for Phase 2 plan and scan handoff
- PRIOR PLAN: docs/plans/2026-04-11T23-19-39Z_PLAN.md

## OPEN QUESTIONS

None
