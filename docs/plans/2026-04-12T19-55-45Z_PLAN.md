# PLAN

## GOAL

`PLAN.md` is being hard-cut from the failed standalone provider-runtime roadmap to a Pi-extension-first contract. The durable outcome for this plan record is a canonical pivot that treats Pi as the runtime, auth, model, and context host while `ozcar` narrows to audit-specific extensions, skills, prompts, JSON contracts, and report/export logic.

This same turn also uses the plan record to drive a bounded rollback: restore the failed Phase 6 live-provider slice to the pre-slice dry-run state without erasing the historical review and execution records that explain why the pivot happened.

## SCOPE

### In Scope

- rewrite `PLAN.md` around project-local Pi extensions, Pi-managed providers, and `/tree`-based context management
- define the next implementation phases for extension scaffolding, audit workflow commands and tools, and JSON-first export/report generation
- roll back the failed Phase 6 live-provider runtime additions from active code
- persist a new repo-local plan artifact and a matching execution artifact for this pivot

### Out of Scope

- implementing the Pi extension scaffold in this turn
- custom provider registration, OAuth work, or model-specific streaming code
- editing sibling repos or upstream Pi repositories
- benchmark harness adapter changes before the Pi-hosted export surface exists

## CONSTRAINTS

- `PLAN.md` remains the canonical implementation contract; `docs/plans/*.md` are records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- keep the audit surface TypeScript-only and JSON-first
- Pi owns auth, provider resolution, model registry, session storage, and tree navigation
- do not copy provider or OAuth internals from `pi-mono`; prefer Pi built-ins and thin `registerProvider()` delegation only if truly required
- keep historical docs artifacts intact even while rolling back the failed Phase 6 code slice

## PHASES

### Phase 1: Canonical Pivot Cut

Objective:
Replace the standalone provider-runner roadmap with a Pi-extension-first contract that names the correct references, boundaries, and next slice.

Deliverables:
- updated `PLAN.md`
- explicit next-phase target of project-local Pi extension scaffolding

### Phase 2: Failed Live-Provider Slice Rollback

Objective:
Restore the active codebase to the pre-Phase-6 dry-run seam so the repo no longer grows a duplicate provider runtime while the extension architecture is adopted.

Deliverables:
- removal or rollback of the live-provider runtime files and tests
- restored dry-run `run`, `plan`, and `scan` paths

### Phase 3: Persisted Closeout

Objective:
Record the pivot and rollback in repo-local artifacts and leave the repo with a validated baseline for the next extension-oriented slice.

Deliverables:
- `docs/plans/2026-04-12T19-55-45Z_PLAN.md`
- one `docs/execs/{UTC_TIMESTAMP}_EXEC.md` referencing this plan

## VALIDATION

- `PLAN.md` no longer targets repo-owned provider preflight, live runtime, or OAuth work as the primary path
- the new phase map identifies the project-local Pi extension scaffold as the next approved implementation slice
- the failed Phase 6 live-provider files are removed or restored to the pre-slice state
- build and test validation are recorded in the execution artifact for this turn
- `docs/plans/2026-04-12T19-55-45Z_PLAN.md` exists and follows the local plan template order

## GATES

- Phase 1 closes when the canonical plan clearly treats Pi as the host runtime and extension scaffold work as the next slice
- Phase 2 may proceed only with a rollback bounded to the failed Phase 6 codepaths and without deleting historical docs artifacts
- Phase 3 closes when validation is recorded in a new execution artifact and the repo is left ready for extension-scaffold implementation

## REFERENCE

- REQUEST: hard pivot to Pi extension workflow and rollback failed Phase 6 provider slice
- PRIOR PLAN: docs/plans/2026-04-12T03-57-14Z_PLAN.md

## OPEN QUESTIONS

None
