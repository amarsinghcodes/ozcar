# PLAN

## GOAL

`PLAN.md` remains the canonical implementation contract for `ozcar`, and the active slice is now Phase 6: live provider runner and preflight. The durable outcome for this `pp` run is a prompt-authoring handoff that turns that canonical Phase 6 contract into one implementation packet and one review packet without reopening planning or pulling later roadmap phases forward.

This repo uses the local artifact workflow for prompt-authoring persistence. This plan artifact records the Phase 6 prompt handoff and points later implementation at `docs/execs/` and later review at `docs/reviews/`, while keeping `PLAN.md` canonical.

## SCOPE

### In Scope

- derive one implementation prompt packet from the canonical Phase 6 slice in `PLAN.md`
- derive one review prompt packet for the resulting Phase 6 implementation artifact
- keep both packets aligned with `AGENTS.md`, the local artifact workflow, and the current post-v1 roadmap
- keep the prompt scope inside live provider runner and preflight work, including live plan or scan execution, durable raw artifacts, and deterministic seam tests
- persist a new repo-local `docs/plans/{UTC_TIMESTAMP}_PLAN.md` record for this prompt-authoring handoff

### Out of Scope

- implementing Phase 6 in this turn
- pulling Phase 7 through Phase 10 work forward into the Phase 6 implementation packet
- editing sibling repos or upstream projects
- benchmark harness or adapter changes before the live runner exists

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are prompt-authoring records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- keep the repo contract TypeScript-only, JSON-first, resumable, and phase-gated
- keep providers behind one small interface and prefer CLI-backed live execution before native OAuth or direct API work
- keep the dry-run fixture seam intact even after live execution lands
- treat paths outside this repo as read-only design references unless the user expands scope

## PHASES

### Phase 1: Phase 6 Implementation Packet

Objective:
Produce a runnable implementation packet that executes the current Phase 6 slice end to end without replanning the roadmap.

Deliverables:
- one implementation packet bounded to live provider runner and preflight work
- explicit repo-local execution artifact guidance under `docs/execs/`

### Phase 2: Phase 6 Review Packet

Objective:
Produce a runnable review packet that inspects the resulting Phase 6 implementation artifact for correctness, regressions, and missing validation.

Deliverables:
- one review packet bounded to review-only work
- explicit repo-local review artifact guidance under `docs/reviews/`

### Phase 3: Prompt-Authoring Closeout

Objective:
Persist this prompt-authoring record and return only the repo-local plan artifact path plus the two prompt packets.

Deliverables:
- `docs/plans/2026-04-12T03-57-14Z_PLAN.md`
- terminal output limited to the persisted artifact path and the two prompt packets

## VALIDATION

- the implementation packet is bounded to Phase 6 and does not pull Phase 7 through Phase 10 forward
- the implementation packet starts with `AGENTS.md` and `PLAN.md`, then points implementation at a fresh `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- the review packet is review-only, findings-first, and points review at the resulting `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- both packets keep the local artifact workflow and template rules explicit
- `docs/plans/2026-04-12T03-57-14Z_PLAN.md` exists and follows the local plan template order

## GATES

- Phase 1 closes when the implementation packet is runnable and bounded to the current Phase 6 contract
- Phase 2 may proceed only after the implementation packet clearly defines the resulting execution artifact surface
- Phase 3 closes when the repo-local plan artifact exists and the terminal response returns only the artifact path plus the two packets

## REFERENCE

- REQUEST: prompt-bank pp
- PRIOR PLAN: docs/plans/2026-04-12T03-52-05Z_PLAN.md

## OPEN QUESTIONS

None
