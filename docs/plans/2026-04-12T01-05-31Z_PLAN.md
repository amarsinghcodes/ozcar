# PLAN

## GOAL

`PLAN.md` remains canonical, and the next active slice is now Phase 3: implement `triage`, `validate`, and `summarize` on top of the completed Phase 1 foundation and the settled Phase 2 provider/plan/scan seam. The immediate goal is to land the smallest Phase 3 implementation that turns raw scan findings into typed triage and validation artifacts, then rebuilds the human-facing reports mechanically from validated JSON only.

This repo still uses the local artifact workflow for persisted execution and review work. This plan artifact records the prompt-authoring handoff for the current Phase 3 slice and keeps later implementation and review packets pointed at the repo-local execution and review templates without replacing `PLAN.md`.

## SCOPE

### In Scope

- Phase 3 prompt sources under `src/prompts/{triager,validator}.md`
- Phase 3 phase logic under `src/phases/{triage,validate,summarize}.ts`
- Phase 3 deterministic gates under `src/gates/{triage,validate}.ts`
- Phase 3 report builders under `src/reports/{summary,confirmed-findings}.ts`
- the minimal command wiring or validation seam needed to exercise typed triage and validation outputs plus mechanical report generation without pulling Phase 4 operational flows forward
- tests and validation for typed JSON sidecars, deterministic gate failures, and report rebuild behavior from validated artifacts only

### Out of Scope

- further Phase 2 changes unless a newly discovered Phase 3 blocker proves the settled seam is insufficient
- Phase 4 resume, replay, rebuild, or doctor completion beyond any minimal seam Phase 3 strictly needs for validation
- Phase 5 hardening beyond the focused seam coverage Phase 3 requires
- public plugin work, duplicated helper trees, or shell/Python orchestration

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `.agents/skills/artifact-md-workflow/SKILL.md` and the local templates for persisted execution and review artifacts
- keep the core runtime TypeScript-only
- keep machine contracts JSON-first and generate Markdown reports from validated JSON only
- keep providers behind one small interface and reuse the settled Phase 2 provider continuity seam rather than reshaping it during Phase 3
- keep rejected and pending findings separated from validated findings
- do not pull Phase 4+ operational flows forward unless Phase 3 validation strictly requires a minimal supporting seam
- treat sibling repos under `../fuzzing-team-foz/` and `../apprentice-victor/` as read-only references

## PHASES

### Phase 1: Triage, Validate, And Summarize Implementation

Objective:
Implement the active `PLAN.md` Phase 3 slice so one dry run can transform scan outputs into typed triage and validation artifacts, keep rejected and pending findings separated, and rebuild `summary.md` plus `confirmed-findings.md` mechanically from validated findings only.

Deliverables:
- triager and validator prompt sources
- `triage`, `validate`, and `summarize` phase modules
- `triage` and `validate` gate modules
- `summary` and `confirmed-findings` report builders
- tests or fixtures that prove typed sidecars, deterministic failures, and report rebuild behavior from validated artifacts only

### Phase 2: Triage, Validate, And Summarize Review

Objective:
Verify the implementation against `PLAN.md`, the local artifact workflow, and the execution artifact before allowing Phase 4 work.

Deliverables:
- a new `docs/reviews/{UTC_TIMESTAMP}_REVIEW.md` targeting the Phase 3 execution artifact
- rerun build, test, and the closest exact validation seam for triage, validation, and report rebuild output
- findings on missing requirements, regressions, overengineering, or drift from the JSON-first report-generation contract

## VALIDATION

- `npm run build` must pass
- `npm test` must pass with added Phase 3 seam coverage
- triage and validation outputs must be stored as typed JSON sidecars under the run store
- `summary.md` and `confirmed-findings.md` must be rebuilt mechanically from validated findings only
- rejected and pending findings must stay separated from validated findings
- gate failures must be deterministic and actionable
- the implementation slice must write `docs/execs/{UTC_TIMESTAMP}_EXEC.md` referencing this plan artifact
- the review slice must confirm Phase 3 scope only and rerun the recorded validation

## GATES

- the implementation slice may start immediately because the latest Phase 2 fix review passed and `PLAN.md` now marks Phase 3 current
- the review slice may start only after the Phase 3 execution artifact exists and the implementation has run the recorded validation
- Phase 4 remains blocked until the Phase 3 review confirms typed sidecars, deterministic gates, and mechanical report generation from validated JSON only

## REFERENCE

- REQUEST: prompt-bank pup for Phase 3 triage, validate, and summarize handoff
- PRIOR PLAN: docs/plans/2026-04-12T00-29-22Z_PLAN.md

## OPEN QUESTIONS

None
