# PLAN

## GOAL

`PLAN.md` remains the canonical contract, and the active slice is Phase 2: project-local Pi extension scaffold. The durable outcome for this prompt-authoring run is a bounded handoff that turns that canonical Phase 2 slice into one implementation packet and one review packet without reopening the architecture pivot or pulling later workflow, provider, or benchmark phases forward.

This repo uses the local artifact workflow for prompt-authoring persistence. This plan artifact records the Phase 2 packet handoff, keeps `PLAN.md` canonical, and points later implementation at `docs/execs/` and later review at `docs/reviews/`.

## SCOPE

### In Scope

- derive one implementation prompt packet for Phase 2 from the canonical plan surface
- derive one review prompt packet for the resulting Phase 2 implementation artifact
- keep both packets aligned with `AGENTS.md`, the local artifact workflow, and the Pi-extension-first contract in `PLAN.md`
- keep the prompt scope inside project-local extension scaffolding, resource discovery, minimal Pi-first commands, and the required validation
- persist a new repo-local `docs/plans/{UTC_TIMESTAMP}_PLAN.md` record for this prompt-authoring handoff

### Out of Scope

- implementing Phase 2 in this turn
- advancing into Phase 3 through Phase 6 work
- reviving the abandoned standalone provider runtime, preflight, or OAuth direction
- editing sibling repos or upstream Pi repos

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- work one phase at a time; do not pull later phases forward unless `PLAN.md` marks them current
- Pi owns auth, provider resolution, model registry, session storage, and tree navigation
- keep the Phase 2 implementation focused on project-local Pi extension scaffolding and avoid extending the standalone CLI or provider runtime
- keep the audit-specific surface TypeScript-only and JSON-first

## PHASES

### Phase 1: Phase 2 Implementation Packet

Objective:
Produce a runnable implementation packet that executes the current Phase 2 slice end to end without reopening the repo’s architectural contract.

Deliverables:
- one implementation packet bounded to `.pi/extensions`, `.pi/skills`, `.pi/prompts`, supporting docs, and required validation
- explicit repo-local execution artifact guidance under `docs/execs/`

### Phase 2: Phase 2 Review Packet

Objective:
Produce a runnable review packet that inspects the resulting Phase 2 implementation artifact for correctness, regressions, and missing validation.

Deliverables:
- one review packet bounded to review-only work
- explicit repo-local review artifact guidance under `docs/reviews/`

### Phase 3: Prompt-Authoring Closeout

Objective:
Persist this prompt-authoring record and return only the repo-local artifact path plus the two prompt packets.

Deliverables:
- `docs/plans/2026-04-12T20-02-43Z_PLAN.md`
- terminal output limited to the plan artifact path and the two prompt packets

## VALIDATION

- the implementation packet is bounded to Phase 2 and does not pull Phase 3 through Phase 6 forward
- the implementation packet starts with `AGENTS.md` and `PLAN.md`, then points implementation at a fresh `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- the review packet is review-only, findings-first, and points review at the resulting `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- both packets keep the local artifact workflow and template rules explicit
- `docs/plans/2026-04-12T20-02-43Z_PLAN.md` exists and follows the local plan template order

## GATES

- Phase 1 closes when the implementation packet is runnable and bounded to the current Phase 2 contract
- Phase 2 may proceed only after the implementation packet clearly defines the resulting execution artifact surface
- Phase 3 closes when the repo-local plan artifact exists and the terminal response returns only the artifact path plus the two packets

## REFERENCE

- REQUEST: prompt-bank pp
- PRIOR PLAN: docs/plans/2026-04-12T19-55-45Z_PLAN.md

## OPEN QUESTIONS

None
