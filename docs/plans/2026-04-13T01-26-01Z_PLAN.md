# PLAN

## GOAL

`PLAN.md` remains the canonical contract for `ozcar`, and this roadmap update reopens repo-local implementation with a clear next slice instead of leaving the repo in a no-active-slice state. The durable outcome is a sequential plan where the next active work comprehensively fixes the remaining Pi-native ergonomics root causes from the latest review, and the following slice extends the export contract with authoritative reported metrics aligned to `../auditor-bench`.

This plan artifact records the canonical closeout of the "Phase 1-6 complete" pause state. Phase 7 becomes the active implementation slice for collision-safe audit ids, human-usable checkpoint/export preparation, and Pi package-first loading. Phase 8 follows it as the next export-contract slice for authoritative reported duration, cost, and token fields.

## SCOPE

### In Scope

- update canonical `PLAN.md` with a current Phase 7 and a subsequent Phase 8
- encode the latest Pi-SOTA review findings as one root-cause ergonomics-hardening slice instead of isolated follow-up fixes
- add Pi package and `../auditor-bench` metric-semantics references and guardrails where the roadmap now depends on them
- persist repo-local plan and execution artifacts for this roadmap update

### Out of Scope

- implementing Phase 7 or Phase 8 code changes
- editing sibling repos such as `../auditor-bench`
- reopening completed Phase 1-6 contracts beyond the sequencing and wording needed for the new active slice

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- keep phases sequential; Phase 8 must remain downstream of Phase 7
- paths outside this repo are read-only design references unless the user expands scope
- preserve the repo's Pi-first, JSON-first, and TypeScript-only guardrails while updating the roadmap

## PHASES

### Phase 1: Canonical Roadmap Reset

Objective:
Update `PLAN.md` so the repo has an explicit active implementation slice that comprehensively fixes the latest review findings and an explicit next slice for authoritative reported metrics export.

Deliverables:
- `PLAN.md` updated with `Phase 7: Pi-Native Ergonomics Hardening` marked current
- `PLAN.md` updated with `Phase 8: Authoritative Reported Metrics Export` marked planned and sequenced after Phase 7
- roadmap guardrails, references, and current-state wording aligned to Pi package conventions and `../auditor-bench` reported metric dimensions

### Phase 2: Artifact Persistence

Objective:
Persist the roadmap update through the repo-local artifact workflow.

Deliverables:
- `docs/plans/2026-04-13T01-26-01Z_PLAN.md`
- `docs/execs/2026-04-13T01-26-02Z_EXEC.md`

## VALIDATION

- `PLAN.md` contains `Phase 7: Pi-Native Ergonomics Hardening` with `Status: current`
- `PLAN.md` contains `Phase 8: Authoritative Reported Metrics Export` after Phase 7 with `Status: planned`
- `PLAN.md` references Pi package conventions and `../auditor-bench` reported metric semantics where the new slices depend on them
- both repo-local artifacts exist and follow the local template order

## GATES

- Phase 1 closes when the canonical roadmap no longer leaves repo-local work without an active slice and Phase 8 is explicitly queued after Phase 7
- Phase 2 closes when both repo-local artifacts exist and reference this roadmap update cleanly

## REFERENCE

- REQUEST: update PLAN.md for Phase 7 and Phase 8
- PRIOR PLAN: docs/plans/2026-04-13T01-11-24Z_PLAN.md

## OPEN QUESTIONS

None
