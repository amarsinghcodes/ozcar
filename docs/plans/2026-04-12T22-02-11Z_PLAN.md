# PLAN

## GOAL

`PLAN.md` remains the canonical contract, and this `pup` run advances that contract from a completed and approved Phase 3 into an active Phase 4 slice. The durable outcome is a clean handoff that retires the Pi-native audit workflow and tree-navigation work as done, promotes JSON artifact export and mechanical report rebuilding to the current implementation phase, and keeps provider-override and benchmark work deferred.

This repo uses the local artifact workflow for prompt-authoring persistence. This plan artifact records the Phase 4 handoff, keeps `PLAN.md` canonical, and points the next implementation and review loops at `docs/execs/` and `docs/reviews/`.

## SCOPE

### In Scope

- update the canonical `PLAN.md` surface so Phase 3 is complete and Phase 4 is current
- derive one implementation prompt packet for the newly active Phase 4 slice
- derive one review prompt packet for the resulting Phase 4 implementation artifact
- keep both packets aligned with `AGENTS.md`, the local artifact workflow, and the Pi-first contract in `PLAN.md`
- keep the prompt scope inside Phase 4 JSON contracts, deterministic artifact storage, mechanical report rebuilding, and the required validation
- persist a new repo-local `docs/plans/{UTC_TIMESTAMP}_PLAN.md` record for this prompt-authoring handoff

### Out of Scope

- implementing Phase 4 in this turn
- reopening the approved Phase 3 audit workflow slice except where `PLAN.md` needs phase-status advancement
- pulling Phase 5 or Phase 6 deliverables forward
- editing sibling repos or upstream Pi repos

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- work one phase at a time; do not pull later phases forward unless `PLAN.md` marks them current
- Pi continues to own auth, provider resolution, model registry, session storage, and tree navigation
- keep the Phase 4 slice focused on repo-owned JSON-first contracts, deterministic rebuilds, and mechanical exports instead of extending provider/runtime seams
- keep the audit-specific surface TypeScript-only and avoid agent-authored canonical reports

## PHASES

### Phase 1: Canonical Plan Advancement

Objective:
Advance the canonical roadmap after the completed and approved Phase 3 review loop.

Deliverables:
- `PLAN.md` updated so Phase 3 is complete and Phase 4 is current
- current-state wording aligned with the approved Phase 3 validation and the newly approved Phase 4 focus

### Phase 2: Phase 4 Implementation Packet

Objective:
Produce a runnable implementation packet for the current Phase 4 slice without pulling Phase 5 or Phase 6 work forward.

Deliverables:
- one implementation packet bounded to shared audit/finding/triage/validation/export contracts, deterministic artifact storage under `.ai-auditor/`, mechanical report builders, and required validation
- explicit repo-local execution artifact guidance under `docs/execs/`

### Phase 3: Phase 4 Review Packet

Objective:
Produce a runnable review packet for the resulting Phase 4 implementation artifact.

Deliverables:
- one review packet bounded to correctness, deterministic rebuild behavior, stale artifact detection, phase discipline, and validation coverage
- explicit repo-local review artifact guidance under `docs/reviews/`

### Phase 4: Prompt-Authoring Closeout

Objective:
Persist this prompt-authoring record and return only the repo-local artifact path plus the two prompt packets.

Deliverables:
- `docs/plans/2026-04-12T22-02-11Z_PLAN.md`
- terminal output limited to the plan artifact path plus the implementation and review prompt packets

## VALIDATION

- `PLAN.md` marks Phase 3 complete and Phase 4 current without altering the later-phase contracts
- the implementation packet is bounded to Phase 4 and does not pull Phase 5 or Phase 6 forward
- the implementation packet starts with `AGENTS.md` and `PLAN.md`, then points implementation at a fresh `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- the review packet is review-only, findings-first, and points review at the resulting `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- both packets keep the local artifact workflow and template rules explicit
- `docs/plans/2026-04-12T22-02-11Z_PLAN.md` exists and follows the local plan template order

## GATES

- Phase 1 closes when the canonical plan clearly retires Phase 3 as complete and promotes Phase 4 to current
- Phase 2 may proceed only after the canonical plan advancement is reflected in `PLAN.md`
- Phase 3 may proceed only after the implementation packet clearly defines the resulting execution artifact surface
- Phase 4 closes when the repo-local plan artifact exists and the terminal response returns only the artifact path plus the two prompt packets

## REFERENCE

- REQUEST: prompt-bank pup
- PRIOR PLAN: docs/plans/2026-04-12T20-49-02Z_PLAN.md

## OPEN QUESTIONS

None
