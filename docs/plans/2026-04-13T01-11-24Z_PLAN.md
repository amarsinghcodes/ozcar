# PLAN

## GOAL

`PLAN.md` remains the canonical contract, and this `pup` run closes the repo-local Phase 6 slice after the export surface, guidance, fixtures, regression coverage, and trivial metadata-drift repair all landed. The durable outcome is a canonical plan surface that marks Phase 6 complete for this repo and records that any remaining downstream adapter migration is external follow-up rather than active `ozcar` implementation work.

This repo uses the local artifact workflow for prompt-authoring persistence. This plan artifact records the closeout decision and the stop condition: no additional in-repo implementation or review packet should be generated until a new request expands scope beyond the completed repo-local Phase 6 seam.

## SCOPE

### In Scope

- update the canonical `PLAN.md` surface so repo-local Phase 6 work is marked complete
- align the Phase 6 acceptance language with the repo-local export/guidance boundary already exercised by the implementation and review loop
- record that downstream adapter migration remains external follow-up outside this repo unless the user expands scope
- persist a new repo-local `docs/plans/{UTC_TIMESTAMP}_PLAN.md` artifact for this prompt-authoring closeout
- stop cleanly without generating implementation or review prompt packets once no active in-repo slice remains

### Out of Scope

- editing sibling repos such as `../auditor-bench`
- inventing a new Phase 7 or reopening Phase 6 contract work inside this repo
- generating synthetic implementation or review packets when the canonical plan has no active in-repo slice
- reshaping internal export contracts, provider ownership, or transcript handling beyond the already-completed Phase 6 seam

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- work one phase at a time; do not pull later phases forward unless `PLAN.md` marks them current
- paths outside this repo are read-only design references unless the user explicitly expands scope
- metadata-only review/fix/plan cross-link drift should be repaired opportunistically inside the active same-phase loop and should not block phase progress by itself unless it misdirects the active slice
- do not leave Phase 6 marked current if the remaining work is downstream-only and outside the approved repo-local scope

## PHASES

### Phase 1: Canonical Phase 6 Closeout

Objective:
Update the canonical roadmap so the completed repo-local Phase 6 seam is reflected accurately.

Deliverables:
- `PLAN.md` updated so Phase 6 is complete instead of current
- Phase 6 acceptance/current-state wording aligned with the implemented repo-local export, fixture, and guidance seam
- explicit note that downstream adapter migration remains external follow-up

### Phase 2: Prompt-Authoring Closeout

Objective:
Persist the closure decision and stop without generating further prompt packets.

Deliverables:
- `docs/plans/2026-04-13T01-11-24Z_PLAN.md`
- terminal response limited to the plan artifact path plus the no-active-slice closure line

## VALIDATION

- `PLAN.md` no longer advertises an active in-repo implementation slice for Phase 6
- the updated Phase 6 wording keeps downstream adapter migration outside this repo unless scope expands
- `docs/plans/2026-04-13T01-11-24Z_PLAN.md` exists and follows the local plan template order
- the terminal response returns only the plan artifact path plus the no-active-slice outcome

## GATES

- Phase 1 closes when the canonical plan marks Phase 6 complete and makes the downstream-only follow-up boundary explicit
- Phase 2 closes when the repo-local plan artifact exists and the terminal response states that no active in-repo implementation slice remains approved

## REFERENCE

- REQUEST: prompt-bank pup
- PRIOR PLAN: docs/plans/2026-04-13T00-40-06Z_PLAN.md

## OPEN QUESTIONS

None
