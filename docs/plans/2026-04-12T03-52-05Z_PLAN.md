# PLAN

## GOAL

`PLAN.md` remains the canonical implementation contract for `ozcar`, and it now needs to reopen cleanly after the completed dry-run v1 so the next approved slice matches the current product reality. The durable outcome for this turn is a canonical roadmap that prioritizes live Claude and Codex execution first, ergonomic audit usage second, optional native OAuth and direct API work third, and benchmark or export work only after live execution exists.

This repo still uses the local artifact workflow for prompt-authoring persistence. This plan artifact records the roadmap rewrite and its sequencing decisions without replacing `PLAN.md` or starting implementation in the same turn.

## SCOPE

### In Scope

- rewrite `PLAN.md` to reflect the post-v1 roadmap instead of the previous closed-plan state
- preserve the completed Phase 1 through Phase 5 foundation while introducing new canonical phases for live execution, ergonomic UX, optional native auth, optional direct API mode, and benchmark export
- align the canonical guardrails with the recommended path from the CritFinder and `pi-mono` review
- add the `pi-mono` and `../auditor-bench/` references that the new roadmap depends on
- persist a new repo-local `docs/plans/{UTC_TIMESTAMP}_PLAN.md` record for this prompt-authoring turn

### Out of Scope

- runtime, contract, fixture, or test changes
- adding dependencies such as `@mariozechner/pi-ai`
- editing `../apprentice-victor/`, `../auditor-bench/`, or upstream `pi-mono`
- execution, fix, or review artifacts for this plan-authoring-only turn

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are prompt-authoring records
- follow `.agents/skills/artifact-md-workflow/SKILL.md` and `.agents/templates/plan.md` for persisted plan work
- keep the repo contract TypeScript-only, JSON-first, resumable, and phase-gated
- sequence parity work as live CLI runner first, ergonomic audit UX second, native auth and direct API third, and benchmark export last
- treat sibling repos and upstream projects as read-only references unless the user expands scope
- do not let `pi-mono` reuse expand into adopting the broader coding-agent runtime

## PHASES

### Phase 1: Canonical Roadmap Rewrite

Objective:
Update `PLAN.md` so the canonical plan no longer ends at the dry-run v1 closure and instead reflects the recommended post-v1 sequence.

Deliverables:
- `PLAN.md` with updated outcome, guardrails, references, target layout, non-goals, validation rules, and post-v1 phases
- explicit Phase 6 through Phase 10 sequencing that keeps native auth and benchmarking behind live execution

### Phase 2: Prompt-Authoring Closeout

Objective:
Persist the roadmap rewrite as a repo-local plan artifact and stop without generating implementation or review artifacts.

Deliverables:
- `docs/plans/2026-04-12T03-52-05Z_PLAN.md`
- terminal closeout that points the next implementation slice at Phase 6

## VALIDATION

- `PLAN.md` explicitly preserves Phases 1 through 5 as complete foundation work
- `PLAN.md` marks the live provider runner as the current implementation slice
- `PLAN.md` places ergonomic audit UX ahead of native OAuth, direct API work, and benchmark export
- `PLAN.md` adds the required CritFinder, `pi-mono`, and `../auditor-bench/` references for the new roadmap
- `docs/plans/2026-04-12T03-52-05Z_PLAN.md` exists and follows the local plan template order

## GATES

- Phase 1 closes when the canonical plan reflects the recommended path comprehensively and unambiguously
- Phase 2 may proceed only after `PLAN.md` is current
- the workflow stops after Phase 2 because this turn updates the canonical roadmap but does not implement Phase 6

## REFERENCE

- REQUEST: update PLAN.md for CritFinder parity and `pi-mono` OAuth path
- PRIOR PLAN: docs/plans/2026-04-12T03-23-40Z_PLAN.md

## OPEN QUESTIONS

- Whether Phase 8 should vendor the small `pi-ai` OAuth seam or depend on `@mariozechner/pi-ai/oauth` through an explicit interop boundary remains a later implementation decision.
- Whether the high-level ergonomic command should be named `audit` or should extend `run` directly remains a Phase 7 naming choice.
