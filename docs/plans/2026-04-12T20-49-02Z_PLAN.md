# PLAN

## GOAL

`PLAN.md` remains the canonical contract, and this `pup` run advances that contract from a completed Phase 2 into an active Phase 3 slice. The durable outcome is a clean handoff that marks the project-local Pi extension scaffold done, promotes audit workflow and tree navigation to the current implementation phase, and keeps later JSON export, provider override, and benchmark work deferred.

This repo uses the local artifact workflow for prompt-authoring persistence. This plan artifact records the Phase 3 handoff, keeps `PLAN.md` canonical, and points the next implementation and review loops at `docs/execs/` and `docs/reviews/`.

## SCOPE

### In Scope

- update the canonical `PLAN.md` surface so Phase 2 is complete and Phase 3 is current
- derive one implementation prompt packet for the newly active Phase 3 slice
- derive one review prompt packet for the resulting Phase 3 implementation artifact
- keep both packets aligned with `AGENTS.md`, the local artifact workflow, and the Pi-first contract in `PLAN.md`
- keep the prompt scope inside Phase 3 audit workflow, Pi session-tree state restoration, and the required validation
- persist a new repo-local `docs/plans/{UTC_TIMESTAMP}_PLAN.md` record for this prompt-authoring handoff

### Out of Scope

- implementing Phase 3 in this turn
- pulling Phase 4 through Phase 6 deliverables forward
- reopening the Phase 2 scaffold or reviving the abandoned standalone provider runtime, preflight, or OAuth direction
- editing sibling repos or upstream Pi repos

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- work one phase at a time; do not pull later phases forward unless `PLAN.md` marks them current
- Pi owns auth, provider resolution, model registry, session storage, and tree navigation
- keep the Phase 3 slice focused on Pi commands, tools, and session-tree-aware state instead of extending the standalone CLI or provider runtime
- keep the audit-specific surface TypeScript-only and JSON-first

## PHASES

### Phase 1: Canonical Plan Advancement

Objective:
Advance the canonical roadmap after the completed and approved Phase 2 scaffold loop.

Deliverables:
- `PLAN.md` updated so Phase 2 is complete and Phase 3 is current
- current-state wording aligned with the completed Phase 2 validation and the newly approved Phase 3 focus

### Phase 2: Phase 3 Implementation Packet

Objective:
Produce a runnable implementation packet for the current Phase 3 slice without pulling later JSON export, provider, or benchmark phases forward.

Deliverables:
- one implementation packet bounded to Pi audit commands, session-backed audit state, tree-navigation conventions, docs, and required validation
- explicit repo-local execution artifact guidance under `docs/execs/`

### Phase 3: Phase 3 Review Packet

Objective:
Produce a runnable review packet for the resulting Phase 3 implementation artifact.

Deliverables:
- one review packet bounded to correctness, regressions, session-tree behavior, phase discipline, and validation coverage
- explicit repo-local review artifact guidance under `docs/reviews/`

### Phase 4: Prompt-Authoring Closeout

Objective:
Persist this prompt-authoring record and return only the repo-local artifact path plus the two prompt packets.

Deliverables:
- `docs/plans/2026-04-12T20-49-02Z_PLAN.md`
- terminal output limited to the plan artifact path plus the implementation and review prompt packets

## VALIDATION

- `PLAN.md` marks Phase 2 complete and Phase 3 current without altering later-phase contracts
- the implementation packet is bounded to Phase 3 and does not pull Phase 4 through Phase 6 forward
- the implementation packet starts with `AGENTS.md` and `PLAN.md`, then points implementation at a fresh `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- the review packet is review-only, findings-first, and points review at the resulting `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- both packets keep the local artifact workflow and template rules explicit
- `docs/plans/2026-04-12T20-49-02Z_PLAN.md` exists and follows the local plan template order

## GATES

- Phase 1 closes when the canonical plan clearly promotes Phase 3 to current and retires Phase 2 as complete
- Phase 2 may proceed only after the canonical plan advancement is reflected in `PLAN.md`
- Phase 3 may proceed only after the implementation packet clearly defines the resulting execution artifact surface
- Phase 4 closes when the repo-local plan artifact exists and the terminal response returns only the artifact path plus the two prompt packets

## REFERENCE

- REQUEST: prompt-bank pup
- PRIOR PLAN: docs/plans/2026-04-12T20-02-43Z_PLAN.md

## OPEN QUESTIONS

None
