# PLAN

## GOAL

`PLAN.md` remains the canonical contract, and this `pup` run advances that contract from a completed and approved Phase 4 into an active Phase 5 slice. The durable outcome is a clean handoff that retires the JSON artifact export and mechanical report work as done, promotes thin Pi-native provider overrides and model presets to the current implementation phase, and keeps benchmark-comparison work deferred.

This repo uses the local artifact workflow for prompt-authoring persistence. This plan artifact records the Phase 5 handoff, keeps `PLAN.md` canonical, and points the next implementation and review loops at `docs/execs/` and `docs/reviews/`.

## SCOPE

### In Scope

- update the canonical `PLAN.md` surface so Phase 4 is complete and Phase 5 is current
- derive one implementation prompt packet for the newly active Phase 5 slice
- derive one review prompt packet for the resulting Phase 5 implementation artifact
- keep both packets aligned with `AGENTS.md`, the local artifact workflow, and the Pi-first contract in `PLAN.md`
- keep the prompt scope inside thin provider overrides, model presets, and the required validation without reopening standalone runtime ownership
- persist a new repo-local `docs/plans/{UTC_TIMESTAMP}_PLAN.md` record for this prompt-authoring handoff

### Out of Scope

- implementing Phase 5 in this turn
- reopening the approved Phase 4 artifact/export slice except where `PLAN.md` needs phase-status advancement
- pulling Phase 6 benchmark-export or comparison work forward
- extending the transition-era standalone CLI provider runtime under `src/providers/*`
- editing sibling repos or upstream Pi repos

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- work one phase at a time; do not pull later phases forward unless `PLAN.md` marks them current
- Pi continues to own auth, provider resolution, model registry, session storage, and tree navigation
- keep the Phase 5 slice focused on optional model presets, audit-specific system-prompt helpers, or thin `registerProvider()` overrides instead of repo-owned auth or streaming work
- any provider-specific extension code must stay behind one small Pi-native module and must not extend the transition-era standalone provider tree unless correctness requires a minimal compatibility touch
- keep the audit-specific surface TypeScript-only and avoid agent-authored canonical reports

## PHASES

### Phase 1: Canonical Plan Advancement

Objective:
Advance the canonical roadmap after the completed and approved Phase 4 fix and review loop.

Deliverables:
- `PLAN.md` updated so Phase 4 is complete and Phase 5 is current
- current-state wording aligned with the approved Phase 4 validation and the newly approved Phase 5 focus

### Phase 2: Phase 5 Implementation Packet

Objective:
Produce a runnable implementation packet for the current Phase 5 slice without pulling Phase 6 work forward.

Deliverables:
- one implementation packet bounded to optional model presets, audit-specific prompt helpers, or thin `registerProvider()` work on the Pi extension surface
- explicit repo-local execution artifact guidance under `docs/execs/`
- explicit guardrails against copied OAuth, custom streaming stacks, or growth of the standalone runtime

### Phase 3: Phase 5 Review Packet

Objective:
Produce a runnable review packet for the resulting Phase 5 implementation artifact.

Deliverables:
- one review packet bounded to correctness, Pi delegation discipline, phase scope, and validation coverage
- explicit repo-local review artifact guidance under `docs/reviews/`

### Phase 4: Prompt-Authoring Closeout

Objective:
Persist this prompt-authoring record and return only the repo-local artifact path plus the two prompt packets.

Deliverables:
- `docs/plans/2026-04-12T23-51-48Z_PLAN.md`
- terminal output limited to the plan artifact path plus the implementation and review prompt packets

## VALIDATION

- `PLAN.md` marks Phase 4 complete and Phase 5 current without altering the later-phase contracts
- the implementation packet is bounded to Phase 5 and does not pull Phase 6 forward
- the implementation packet starts with `AGENTS.md` and `PLAN.md`, then points implementation at a fresh `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- the review packet is review-only, findings-first, and points review at the resulting `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- both packets keep the local artifact workflow and template rules explicit
- `docs/plans/2026-04-12T23-51-48Z_PLAN.md` exists and follows the local plan template order

## GATES

- Phase 1 closes when the canonical plan clearly retires Phase 4 as complete and promotes Phase 5 to current
- Phase 2 may proceed only after the canonical plan advancement is reflected in `PLAN.md`
- Phase 3 may proceed only after the implementation packet clearly defines the resulting execution artifact surface
- Phase 4 closes when the repo-local plan artifact exists and the terminal response returns only the artifact path plus the two prompt packets

## REFERENCE

- REQUEST: prompt-bank pup
- PRIOR PLAN: docs/plans/2026-04-12T22-02-11Z_PLAN.md

## OPEN QUESTIONS

None
