# PLAN

## GOAL

`PLAN.md` remains canonical, and the next active slice is now Phase 5: verification hardening and v1 readiness. The immediate goal is to harden the highest-risk filesystem seams left after the completed Phase 4 operational CLI work by adding fixture-backed coverage and one sample end-to-end run fixture, without widening the runtime surface or disturbing the settled JSON-first artifact contract.

This repo still uses the local artifact workflow for persisted execution and review work. This plan artifact records the prompt-authoring handoff for the current Phase 5 slice and keeps later implementation and review packets pointed at the repo-local execution and review templates without replacing `PLAN.md`.

## SCOPE

### In Scope

- Phase 5 seam coverage for provider detection, parser or dedup behavior, replay, gate failures, and report rebuilds
- one sample end-to-end run fixture that proves resumability and deterministic report generation from durable artifacts
- minimal fixture or test-helper updates required to validate those behaviors against the existing filesystem-first runtime
- repo-local execution and review artifacts for the Phase 5 implementation and review loops

### Out of Scope

- reopening completed Phase 1 through Phase 4 runtime behavior unless a current Phase 5 seam exposes a real blocker
- new product features, provider expansions, plugin work, duplicated helper trees, or shell or Python orchestration
- polish-only metadata churn unrelated to the Phase 5 hardening acceptance criteria

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `.agents/skills/artifact-md-workflow/SKILL.md` and the local templates for persisted execution and review artifacts
- keep the core runtime TypeScript-only
- keep machine contracts JSON-first and regenerate Markdown reports from validated JSON only
- keep providers behind one small interface and prefer fixture-backed seam tests over runtime reshaping
- Phase 5 is hardening, not surface-area expansion
- treat sibling repos under `../fuzzing-team-foz/` and `../apprentice-victor/` as read-only references

## PHASES

### Phase 1: Verification Hardening Implementation

Objective:
Implement only the active `PLAN.md` Phase 5 slice by adding the missing hardening coverage around provider detection, parser or dedup behavior, replay, gate failures, report rebuilds, and one sample end-to-end run fixture that proves resumability and deterministic report regeneration.

Deliverables:
- fixture-backed component tests for provider detection, parser or dedup behavior, replay, gate failures, and report rebuilds
- a sample end-to-end run fixture that can be replayed and rebuilt without manual patching
- minimal fixture or test-harness updates required to keep the v1 surface TypeScript-only and phase-gated

### Phase 2: Verification Hardening Review

Objective:
Verify the Phase 5 implementation against `PLAN.md`, the local artifact workflow, and the execution artifact before treating the v1 hardening slice as complete.

Deliverables:
- a new `docs/reviews/{UTC_TIMESTAMP}_REVIEW.md` targeting the Phase 5 execution artifact
- rerun build, tests, and the closest exact validation seams recorded by the Phase 5 execution artifact
- findings on coverage gaps, regressions, overengineering, or drift from the JSON-first filesystem contract

## VALIDATION

- `npm run build` must pass
- `npm test` must pass with added Phase 5 seam coverage
- fixture-backed tests must cover provider detection, parser or dedup behavior, replay, gate failures, and report rebuilds
- a sample run fixture must be replayable and rebuildable without manual patching and must prove resumability
- the implementation slice must write `docs/execs/{UTC_TIMESTAMP}_EXEC.md` referencing this plan artifact
- the review slice must confirm Phase 5 scope only and rerun the recorded validation

## GATES

- the implementation slice may start immediately because the Phase 4 implementation review passed and the follow-up fix review closed the remaining Phase 4 coverage gap
- the review slice may start only after the Phase 5 execution artifact exists and the implementation has run the recorded validation
- any v1-ready follow-up planning remains blocked until the Phase 5 review confirms the hardening slice against durable filesystem state

## REFERENCE

- REQUEST: prompt-bank pup for Phase 5 verification hardening handoff
- PRIOR PLAN: docs/plans/2026-04-12T02-05-07Z_PLAN.md

## OPEN QUESTIONS

None
