# PLAN

## GOAL

`PLAN.md` remains the canonical contract, and this `pup` run advances that contract from a completed and approved Phase 5 into an active Phase 6 slice. The durable outcome is a clean handoff that retires the thin Pi-native provider/model ergonomics work as done, promotes benchmark export and comparative verification to the current implementation phase, and keeps the internal JSON/export seam stable while downstream comparison work comes online.

This repo uses the local artifact workflow for prompt-authoring persistence. This plan artifact records the Phase 6 handoff, keeps `PLAN.md` canonical, and points the next implementation and review loops at `docs/execs/` and `docs/reviews/`.

## SCOPE

### In Scope

- update the canonical `PLAN.md` surface so Phase 5 is complete and Phase 6 is current
- derive one implementation prompt packet for the newly active Phase 6 slice
- derive one review prompt packet for the resulting Phase 6 implementation artifact
- keep both packets aligned with `AGENTS.md`, the local artifact workflow, and the Pi-first contract in `PLAN.md`
- keep the prompt scope inside stable `findings.json` export consumption, downstream adapter notes or fixtures, and explicit comparison assumptions without reshaping the internal export contracts
- persist a new repo-local `docs/plans/{UTC_TIMESTAMP}_PLAN.md` record for this prompt-authoring handoff

### Out of Scope

- implementing Phase 6 in this turn
- reopening the approved Phase 5 preset/provider slice except where `PLAN.md` needs phase-status advancement
- reintroducing repo-owned auth, streaming, provider runtime ownership, or transcript scraping
- reshaping the internal JSON artifact contracts to satisfy downstream adapters when notes or fixtures are sufficient
- editing sibling repos or upstream Pi repos

## CONSTRAINTS

- `PLAN.md` remains canonical; `docs/plans/*.md` are records
- follow `AGENTS.md`, `.agents/skills/artifact-md-workflow/SKILL.md`, and the checked-in local templates
- work one phase at a time; do not pull later phases forward unless `PLAN.md` marks them current
- Pi continues to own auth, provider resolution, model registry, session storage, and tree navigation
- keep Phase 6 focused on the stable export surface and comparative verification notes, not on new provider/runtime or transcript-management work
- downstream comparison work must consume exported validated findings instead of Pi session transcripts
- keep the audit-specific surface TypeScript-only and avoid agent-authored canonical reports

## PHASES

### Phase 1: Canonical Plan Advancement

Objective:
Advance the canonical roadmap after the completed and approved Phase 5 fix and review loop.

Deliverables:
- `PLAN.md` updated so Phase 5 is complete and Phase 6 is current
- current-state wording aligned with the approved Phase 5 validation and the newly approved Phase 6 focus

### Phase 2: Phase 6 Implementation Packet

Objective:
Produce a runnable implementation packet for the current Phase 6 slice without reopening the provider/runtime seam.

Deliverables:
- one implementation packet bounded to stable export-surface hardening, adapter notes or fixtures, and explicit comparison assumptions on validated findings
- explicit repo-local execution artifact guidance under `docs/execs/`
- explicit guardrails against transcript scraping, provider/runtime reshaping, or backward pressure on the internal JSON contracts

### Phase 3: Phase 6 Review Packet

Objective:
Produce a runnable review packet for the resulting Phase 6 implementation artifact.

Deliverables:
- one review packet bounded to correctness, contract stability, downstream-consumer discipline, and validation coverage
- explicit repo-local review artifact guidance under `docs/reviews/`

### Phase 4: Prompt-Authoring Closeout

Objective:
Persist this prompt-authoring record and return only the repo-local artifact path plus the two prompt packets.

Deliverables:
- `docs/plans/2026-04-13T00-40-06Z_PLAN.md`
- terminal output limited to the plan artifact path plus the implementation and review prompt packets

## VALIDATION

- `PLAN.md` marks Phase 5 complete and Phase 6 current without altering the later-phase contract boundaries
- the implementation packet is bounded to Phase 6 and does not reopen provider/runtime ownership or transcript scraping
- the implementation packet starts with `AGENTS.md` and `PLAN.md`, then points implementation at a fresh `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- the review packet is review-only, findings-first, and points review at the resulting `docs/execs/{UTC_TIMESTAMP}_EXEC.md`
- both packets keep the local artifact workflow and template rules explicit
- `docs/plans/2026-04-13T00-40-06Z_PLAN.md` exists and follows the local plan template order

## GATES

- Phase 1 closes when the canonical plan clearly retires Phase 5 as complete and promotes Phase 6 to current
- Phase 2 may proceed only after the canonical plan advancement is reflected in `PLAN.md`
- Phase 3 may proceed only after the implementation packet clearly defines the resulting execution artifact surface
- Phase 4 closes when the repo-local plan artifact exists and the terminal response returns only the artifact path plus the two prompt packets

## REFERENCE

- REQUEST: prompt-bank pup
- PRIOR PLAN: docs/plans/2026-04-12T23-51-48Z_PLAN.md

## OPEN QUESTIONS

None
