{
  "name": "ozcar",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 foundation for the ozcar audit runner",
  "bin": {
    "ozcar": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.pi.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
